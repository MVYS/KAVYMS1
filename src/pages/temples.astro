---
import Base from '../layouts/Base.astro';
---
<Base title="Vasavi Temples" description="Directory of Vasavi temples — search, filter and view on map.">
  <h1 class="section-title">Vasavi Temples</h1>
  <p style="text-align:center;color:var(--color-text-soft);max-width:680px;margin:0 auto 24px">
    Find a Vasavi temple — search by name, filter by state or city, or switch to the map view.
  </p>
  <div data-directory>
    <div class="state state-loading">Loading temples…</div>
  </div>
</Base>

<script>
  import { mountDirectory } from '../lib/directory';
  const root = document.querySelector<HTMLElement>('[data-directory]')!;
  mountDirectory(root, { sheet: 'temples', noun: 'temple' });
</script>
